{
  "rules": {
    ".read": false,
    ".write": false,
    "rooms": {
      ".read": true,
      ".write": true
    },
    "training": {
      "pointClips": {
        ".read": "auth != null && auth.uid === 'REPLACE_WITH_YOUR_FIREBASE_UID'",
        "$day": {
          "$clipId": {
            ".write": "!data.exists()"
          }
        }
      }
    }
  }
}
